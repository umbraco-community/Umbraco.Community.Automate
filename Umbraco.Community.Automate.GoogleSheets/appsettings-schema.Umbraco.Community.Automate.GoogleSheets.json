{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "UmbracoCommunityAutomateGoogleSheetsSchema",
  "type": "object",
  "properties": {
    "Umbraco": {
      "description": "Configuration container for all Umbraco products.",
      "oneOf": [
        {
          "$ref": "#/definitions/UmbracoDefinition"
        }
      ]
    }
  },
  "definitions": {
    "UmbracoDefinition": {
      "type": "object",
      "properties": {
        "Automate": {
          "description": "Configuration of Umbraco Automate.",
          "oneOf": [
            {
              "$ref": "#/definitions/UmbracoAutomateDefinition"
            }
          ]
        }
      }
    },
    "UmbracoAutomateDefinition": {
      "type": "object",
      "properties": {
        "Providers": {
          "description": "OAuth provider credentials, keyed by provider name.",
          "oneOf": [
            {
              "$ref": "#/definitions/ProvidersDefinition"
            }
          ]
        }
      }
    },
    "ProvidersDefinition": {
      "type": "object",
      "properties": {
        "Google": {
          "description": "Google OAuth app credentials and scope configuration.",
          "oneOf": [
            {
              "$ref": "#/definitions/OAuthProviderConfiguration"
            }
          ]
        }
      }
    },
    "OAuthProviderConfiguration": {
      "type": "object",
      "description": "Configuration for an OAuth provider's application credentials.\nThese are the client ID/secret registered with the external provider (e.g. a Google OAuth App),\nnot the user's access tokens.",
      "properties": {
        "ClientId": {
          "type": [
            "null",
            "string"
          ],
          "description": "Gets or sets the OAuth client ID."
        },
        "ClientSecret": {
          "type": [
            "null",
            "string"
          ],
          "description": "Gets or sets the OAuth client secret."
        },
        "Scopes": {
          "type": "array",
          "description": "Gets or sets the OAuth scopes to request.",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}
