pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged --config .gitleaks.toml --redact --verbose

pre-push:
  scripts:
    "gitleaks-scan-range.sh":
      runner: bash
      use_stdin: true
